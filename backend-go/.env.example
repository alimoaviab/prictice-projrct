PORT=8080
APP_NAME=school

# Replace with a strong random string for any non-local environment.
JWT_SECRET=dev-only-jwt-secret-change-me

# Comma-separated list of origins allowed by the CORS middleware.
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Set to "true" in production so the session cookie carries the Secure flag.
COOKIE_SECURE=false

# PostgreSQL connection string. Leave empty for pure in-memory mode.
# DATABASE_URL=postgres://school_user:school_password@localhost:5432/school_db?sslmode=disable

# Redis connection string. Leave empty to run without caching.
# REDIS_URL=redis://localhost:6379/0
