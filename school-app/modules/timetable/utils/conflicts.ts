import { TimetableFormInput, TimetableRecord } from "../types/timetable.types";

export function findTimetableConflicts(
    records: TimetableRecord[],
    input: TimetableFormInput,
    excludedRecordId?: string
): TimetableRecord[] {
    void excludedRecordId;
    void input;
    void records;
    return [];
}